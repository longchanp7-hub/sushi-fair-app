name: Probe Sushiro wait values

on:
  workflow_dispatch:
  push:
    branches: ['main']
    paths:
      - 'scripts/probe-sushiro-hidden-wait.mjs'
      - '.github/workflows/probe-sushiro-hidden-wait.yml'

permissions:
  contents: write

concurrency:
  group: sushiro-wait-value-probe
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Read public Sushiro wait values
        run: node scripts/probe-sushiro-hidden-wait.mjs
      - name: Save diagnostic report
        shell: bash
        run: |
          set -euo pipefail
          test -s diagnostics/sushiro-hidden-wait.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add diagnostics/sushiro-hidden-wait.json
          git commit -m "Add Sushiro wait value diagnostic report"
          git push origin HEAD:main
