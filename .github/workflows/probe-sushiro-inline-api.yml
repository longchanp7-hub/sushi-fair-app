name: Probe Sushiro inline API calls

on:
  workflow_dispatch:
  push:
    branches: ['main']
    paths:
      - 'scripts/probe-sushiro-inline-api.mjs'
      - '.github/workflows/probe-sushiro-inline-api.yml'

permissions:
  contents: write

concurrency:
  group: sushiro-inline-api-probe
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Inspect public Sushiro inline scripts
        run: node scripts/probe-sushiro-inline-api.mjs
      - name: Save diagnostic report
        shell: bash
        run: |
          set -euo pipefail
          test -s diagnostics/sushiro-inline-api.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add diagnostics/sushiro-inline-api.json
          git commit -m "Add Sushiro inline API diagnostic report"
          git push origin HEAD:main
